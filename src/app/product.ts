export class Product {

    constructor(public productID: number, public name: string, public description: string){
        this.productID = productID;
        this.name = name;
        this.description = description
    }

}
